---
interface Props {
  title?:string,
  description?:string,
  img?:object
}
//los ? indica que puede ser opcional

//recibo un objeto global de astro para acceder a la propiedad Pops, es un objeto por lo tanto puedo extraer sus valores
const {title='Titulo por defecto',description='descripcion de ejemplo'}=Astro.props;
console.log (title);
console.log (description);

---

<div class=" size-full bg-nav shadow-lg rounded-lg p-5 dark:bg-neutral-900 border-2 border-yellow-500">
  <div class="flex items-center gap-x-4 mb-3">
    <div class="inline-flex justify-center items-center size-[62px] rounded-full border-2 border-yellow-500  dark:border-blue-900 dark:bg-blue-800">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-8">
        <path fill-rule="evenodd" d="M15.75 4.5a3 3 0 1 1 .825 2.066l-8.421 4.679a3.002 3.002 0 0 1 0 1.51l8.421 4.679a3 3 0 1 1-.729 1.31l-8.421-4.678a3 3 0 1 1 0-4.132l8.421-4.679a3 3 0 0 1-.096-.755Z" clip-rule="evenodd" />
      </svg>
      
    </div>
    <div class="flex-shrink-0">
      <h3 class="block text-lg font-semibold text-yellow-500 dark:text-white">{title}</h3>
    </div>
  </div>
    <p class="text-white dark:text-neutral-400 pb-2">{description}</p>
</div>